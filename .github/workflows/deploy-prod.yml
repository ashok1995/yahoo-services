name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  ci:
    name: CI Gate
    uses: ./.github/workflows/ci.yml

  deploy:
    name: Deploy to Production
    runs-on: self-hosted
    needs: ci
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Verify branch
        run: |
          BRANCH=$(git branch --show-current || echo "$GITHUB_REF_NAME")
          echo "Deploying branch: $BRANCH"

      - name: Stop existing production containers
        run: docker compose --profile prod down 2>/dev/null || true

      - name: Build production image (no cache)
        run: docker compose --profile prod build --no-cache

      - name: Start production service
        run: docker compose --profile prod up -d

      - name: Wait for service
        run: |
          echo "Waiting for production service on port 8185..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8185/health > /dev/null 2>&1; then
              echo "Service is up after ${i}s"
              break
            fi
            sleep 1
          done

      - name: Health check
        run: |
          RESPONSE=$(curl -sf http://localhost:8185/health)
          STATUS=$(echo "$RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin)['status'])")
          echo "Health status: $STATUS"
          [ "$STATUS" = "healthy" ] || exit 1

      - name: Smoke test global-context
        run: |
          RESPONSE=$(curl -sf http://localhost:8185/api/v1/global-context)
          python3 -c "
          import sys, json
          data = json.loads('''$RESPONSE''')
          required = ['sp500','nasdaq','dow_jones','vix','gold','usd_inr','crude_oil']
          for key in required:
              assert key in data, f'Missing {key}'
              print(f'{key}: OK', end='')
              if data[key].get('trend'):
                  regime = data[key]['trend']['short_term']['regime']
                  print(f' (trend: {regime})', end='')
              print()
          print('Production smoke test passed')
          "

      - name: Print deployment info
        if: success()
        run: |
          echo "=========================================="
          echo "  PRODUCTION deployed successfully"
          echo "  Port: 8185"
          echo "  Health: http://localhost:8185/health"
          echo "  API:    http://localhost:8185/api/v1/global-context"
          echo "  Docs:   http://localhost:8185/docs"
          echo "=========================================="
